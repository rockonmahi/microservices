server:
  port: 5117
eureka:
  client:
    serviceUrl:
      defaultZone: "http://registry-service:5112/eureka/"
management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: "http://zipkin:9411/api/v2/spans"
logging:
  level:
    org:
      springframework:
        security: TRACE
  pattern:
    console: "%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n"
spring:
  security:
    saml2:
      relyingparty:
        registration:
          carsonline:
            signing:
              credentials:
                - private-key-location: classpath:credentials/private.key
                  certificate-location: classpath:credentials/certificate.crt
            decryption:
              credentials:
                - private-key-location: classpath:credentials/private.key
                  certificate-location: classpath:credentials/certificate.crt
            assertingparty:
              metadata-uri: classpath:okta-metadata.xml